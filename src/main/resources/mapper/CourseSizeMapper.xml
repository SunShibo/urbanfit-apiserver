<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.urbanfit.apiserver.dao.CourseSizeDao">
    <select id="queryCourseSize" parameterType="int" resultType="com.urbanfit.apiserver.entity.CourseSize">
        select * from t_course_size where courseId = #{courseId}
    </select>
</mapper>